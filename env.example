# Example environment configuration for the iCloud MCP server
#
# Copy this file to `.env` and fill in your Apple ID (email address) and
# an app‑specific password generated in the Apple ID settings.  Without
# these credentials the server cannot connect to iCloud services.

# REQUIRED CREDENTIALS
APPLE_ID=your-apple-id@example.com
ICLOUD_APP_PASSWORD=your-app-specific-password

# OPTIONAL MAIL SETTINGS
# Override IMAP/SMTP hostnames or ports if you use a custom setup.  The
# defaults point at the standard iCloud mail servers.
# IMAP_SERVER=imap.mail.me.com
# IMAP_PORT=993
# SMTP_SERVER=smtp.mail.me.com
# SMTP_PORT=587
# SMTP_USE_SSL=false

# OPTIONAL CALDAV SETTINGS
# The default URL points to the global iCloud CalDAV endpoint.  Do not
# modify this unless instructed by Apple.
# CALDAV_URL=https://caldav.icloud.com

# TZID specifies the default timezone used when creating new calendar
# events.  It should be an IANA timezone identifier (see
# https://en.wikipedia.org/wiki/List_of_tz_database_time_zones).
# TZID=America/New_York

# MAILBOX NAMES
# If your account uses localized folder names, adjust these to match.
# ARCHIVE_MAILBOX=Archive
# TRASH_MAILBOX=Trash
# DRAFTS_MAILBOX=Drafts
# SENT_MAILBOX=Sent

# SERVER BINDING
# HOST and PORT control where the MCP server listens.  By default it
# binds to 127.0.0.1:8000.  When exposing to the internet, do so only
# behind HTTPS and proper authentication.
# HOST=127.0.0.1
# PORT=8000

# OAUTH AUTHENTICATION
# OAuth is enabled by default and required to connect from ChatGPT.
# Set a password to protect the consent screen before starting the server.
OAUTH_CONSENT_PASSWORD=choose-a-strong-password

# Advanced OAuth overrides (optional).  Uncomment to customise behaviour.
# OAUTH_ENABLED=true
# OAUTH_CLIENT_STORE=oauth_clients.json
# OAUTH_BASE_URL=http://127.0.0.1:8000
# OAUTH_ISSUER_URL=http://127.0.0.1:8000
# OAUTH_DEFAULT_SCOPES=icloud.full
# OAUTH_VALID_SCOPES=icloud.full
# OAUTH_REQUIRED_SCOPES=icloud.full
# OAUTH_PENDING_TTL=600
# OAUTH_AUTH_CODE_TTL=600
# OAUTH_ACCESS_TOKEN_TTL=3600
# OAUTH_REFRESH_TOKEN_TTL=604800

# ADDITIONAL CALENDAR SEARCH SETTINGS
# SCAN_DAYS defines how far into the past and future the calendar
# search functions (`search_events` and `fetch_events`) scan when
# looking for events.  The default of 3*365 (3 years) should cover
# most use cases.  Adjust if you need a wider or narrower window.
# SCAN_DAYS=1095
